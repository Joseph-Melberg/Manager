apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-heartbeat-listener.fullname" . }}-test-connection"
  labels:
    {{- include "helm-heartbeat-listener.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-heartbeat-listener.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
